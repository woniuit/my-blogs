#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_stylus@0.58.1@stylus/bin/stylus" "$@"
  ret=$?
else 
  node  "$basedir/../_stylus@0.58.1@stylus/bin/stylus" "$@"
  ret=$?
fi
exit $ret
